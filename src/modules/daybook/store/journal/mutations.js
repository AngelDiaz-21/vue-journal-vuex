// Son sincronas y hacen la modificacion del state, el state es reactivo y cuando se cambia el state vamos a notificar a todos los componentes o a varios lugares donde esten escuchando los cambios del state

// Cuando se cargan las peticiones se deben de establecer esas entradas 
export const setEnties = (/*state*/) => {

}

// Cuando se hace una actualizacion y se hace modificacion en la base de datos se va a regresar esta funcion
export const updateEntry = (/*state*/) => {

}

// Para agregar una nueva entrada
export const addEntry = (/*state*/) => {

}