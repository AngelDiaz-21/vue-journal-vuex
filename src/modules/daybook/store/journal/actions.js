// Pueden ser acciones asincronas que van a mandar a llamar una mutation 

// Esto van a estar haciendo impacto contra la base de datos
// Accion para las cargas de las entradas
export const loadEntries = async (/*{ commit }*/ ) => {

}

// Vamos a ocupar una actualizacion
export const updateEntry = async (/*{ commit }*/ ) => {
    
}

// Creacion de la entrada
export const createEntry = async (/*{ commit }*/ ) => {

}